spring.datasource.url=jdbc:mysql://localhost:3306/future_proof_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# Above is the local database, potential error
# Default Profile Configuration
# Set active profile: dev or prod
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Application Name
spring.application.name=gallery

# Common Configuration (can be overridden by profile-specific properties)
# Profile-specific configurations are in:
# - application-dev.properties (development)
# - application-prod.properties (production)
